package com.ai_engineering.agent_service.client;

/**
 * Seam for the future MCP Tool Service.
 *
 * <p><b>Not yet implemented.</b> The {@code mcp_service} project exists but
 * exposes no endpoints yet. When it lands, add an implementation that invokes
 * external tools (docs, GitHub, third-party APIs) on behalf of the agent so
 * tool results can be folded into the orchestration flow.
 *
 * <p>Defined now purely to document the orchestration seam; it has no methods
 * until the contract is known.
 */
public interface McpServicePort {
    // Intentionally empty: placeholder for the MCP Service contract.
}
